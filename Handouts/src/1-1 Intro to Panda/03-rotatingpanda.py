# intro to Panda/03-rotatingpanda.py
from Panda import *
a = slider(min = 0, max = 2*pi, label = "angle")
# Create a panda at (0,0,0) and play with hpr using this slider
# Use "HPR" to create an HPR object

# Note that pi is a defined constant

# Change this to alter pitch and roll


start()