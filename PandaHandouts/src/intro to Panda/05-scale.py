from Panda import *
h = slider(max = 5)
m = slider (min = -5, max = 5)
panda(position = P3 (0,0,0), size = h)
panda(position = P3 (0,0,0), size = m, )
# Create 2 pandas at (1,0,0) and (-1,0,0) and set their sizessize
# using sliders that range 0 to 5 and init at 1
# Display the values of the sizes in text objects
# What happens when the pandas touch?
# What happens when the size is negative?


start()

