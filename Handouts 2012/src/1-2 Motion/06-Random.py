from Panda import *

# You can use random numbers to create interesting effects in conjunction with a for loop.

# randomRange(1, 4) - This creates a random number between 1 and 4

panda(position = P3(randomRange(-3, 3), 0, 0))

# Change this to make 10 pandas at random locations.
# Then randomize the z coordinate.
# Then make the panda move from 0, 0, 0 to the random location in a random amount of time between 1 and 4

start()
